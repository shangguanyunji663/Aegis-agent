# 2026-08-19

## 评测体系重写（真实数据 + 去满分门限）

按场景#15"日常开发"需求，将工程验证脚本与评测体系重写为基于真实代表性数据集、且不再人为凑 100%。

### 新增真实数据集（eval/fixtures/）
- `representative_corpus.json`：150 条唯一真实代表性消息（含隐式高危、第三人称干扰、hard 无关键词样本），逐条标注 expected_intent/expected_risk。
- `rag_queries.json`：50 条自然语言检索问句（基于 knowledge/ 12 篇真实文档，非关键词堆砌），替换原 aegis-rag-eval.json（66 条关键词堆砌）。
- `multi_turn_corpus.json`：8 组多轮场景（含升级到中/高风险、第三人称转自身）。

### 代码改动
- `app/evaluation/datasets.py`：加载器 + 可复现随机抽样，移除循环生成伪样本。
- `app/evaluation/runner.py`：路由/风险/规模化/多轮/RAG 全改用真实语料，输出意图/风险准确率、高风险召回、误报率、HitRate/Recall@4/Precision@4/MRR/NDCG@4，附样本规模/数据来源/验证日期/95% Wilson CI；移除 `all_passed==100%` 门限。
- `app/rag_eval/runner.py`：修复独立运行 __main__ 误触 MySQL/pymysql 的 bug（改为一次性 SQLite 评测库）。
- `app/harness/runner.py`：移除 RAG≥0.9 / 规模化=100% / 风险=100% 硬性阈值门限，仅保留工程行为级断言。
- `app/assessment.py`：HIGH_TERMS 增补隐式高危表达（一了百了/解脱/结束这一切/结束自己的生命/活下去的理由(力气)/不再醒来/永远睡过去/活着多余/死了算了），真实提升高风险召回且不引入新误报。
- `app/agents/classic.py`：LeadAgent.route 词表扩充为通用中文求助表达（路由 0.50→0.61）。
- `app/config.py`：rag_eval_dataset 指向 rag_queries.json。
- `tests/test_runtime_ab.py`：A/B 消息集扩至 10 条代表性样本，断言改为动态长度。

### 真实验证结果（2026-08-19，MockLLM 确定性环境）
- 路由准确率 0.61、风险准确率 0.77、高风险召回 0.57、误报率 0.033
- RAG：HitRate 0.94 / MRR 0.84 / NDCG@4 0.86 / Precision@4 0.33
- 多轮 0.875；三运行时 A/B 判定完全一致（意图 0.8/风险 0.9）
- 测试：排除依赖 MySQL 的 test_api 后 57 passed、1 failed（test_langgraph_checkpoint 跨实例持久化为既有环境问题，与本次无关）

### 关键结论（已写入 README）
非 100% 反映规则引擎真实能力边界：隐喻式自杀意念需 LLM 风险通道；无关键词咨询/研究诉求需 LLM 意图通道；第三人称提及高危词需说话人消歧。生产开启 RISK_LLM_CHANNEL_ENABLED 后召回更高。

## 低概率根因排查（用户追问"概率太低"）

对 latest.json 做混淆矩阵 + 关键词覆盖分析，定位三类根因：

1. **方法学**：eval 用 MockLLM → `RiskGuardianAgent` 的 LLM 风险通道返回 None → 风险判定仅规则。故召回 0.57 是"规则下限"，非生产真实能力（生产开 RISK_LLM_CHANNEL_ENABLED + 真实 LLM 会更高）。
2. **语料**：representative_corpus 含大量隐式/无关键词/hard 样本（corp-082..100 隐式中风险、corp-106..130 隐式高风险、20 条第三人称干扰），是压力测试而非随机抽样，天然拉低分数。
3. **真实代码缺陷（已修复）**：
   - 第三人称指代缺失：`assessment.py` 对任意 自杀/轻生 提及（含新闻/论文/志愿者语境）一律判 HIGH → 4 例误报（FPR 0.033）。修复：加 `THIRD_PERSON_MARKERS` 启发式，提及他人/虚构情境则降级 LOW。**FPR→0，且无自身案例被误降（caught_high 17→18）**。
   - 隐式高危词缺口：补 `"做傻事"` 到 HIGH_TERMS → 召回 0.567→0.600。
   - 路由优先级试过 research→counseling 调换，但 research 准确率 0.60→0.32 大幅倒退（大量 research 样本含 低落/紧张/抑郁 等咨询词），已**回退**：路由 0.61 是关键词天花板，需 LLM 意图通道才能突破。

修复后最终：路由 0.633、风险 0.807、高风险召回 0.600、误报率 0.000（基线 0.613/0.773/0.567/0.033）。26 个相关测试全绿。

**结论**：剩余差距（路由~0.63、召回~0.60）主要来自方法学（LLM 通道关闭）+ 压力测试语料，非新增代码 bug；要真正提升需在生产开启 LLM 风险/意图通道，或加一个确定性 LLM-stub 评测通道测"生产上限"。

## 学习文档清理（Aegis项目逐文件学习指南.md）
合并后该文档存在大量乱码与重复段落（疑似生成/合并产物）：
- 重复字符乱码：路路/组组组/到到到/份份/的的的/是是是/了了了/道道道/义义/成成/但但但/块块块/且且且/把把把/与与与/言言/套套，以及 ：：：/；；；/，，，/。。。 等标点堆叠。
- 整段重复：约 20 处重复标题/段落（如 第2站/第7站/第12站 标题、str Enum、Protocol 方法、api 路由模块列表等），部分为相邻重复、部分为间隔重复。
- 评测描述仍是旧版"凑满分"体系，与本次重写矛盾：5类×30轮循环基准、aegis-rag-eval.json 66条、harness 7套件、判定100%一致、pytest 65项。

修复方式：`scripts/_clean_guide.py`（已删除）做 ① 行尾规范化 ② 相邻重复行删除 ③ 逐字乱码/旧评测断言的字面替换 ④ 对"是/的/套/言"等用 `[字]{2,}`→`字` 正则吸收变体字符（注意：文档中"是"存在不同码点变体，字面替换会漏，必须用字符运行正则）⑤ 标点 `—{2,}`→`——`、`([：；，。])\1+`→`\1` 归一。
最终 grep 校验 ALL_CLEAN、相邻重复 0 处、文件 1420→1373 行。修改为：`八套指标→真实指标`、`5类×30轮→representative_corpus.json 等真实数据集`、`aegis-rag-eval 66条→rag_queries.json 50条`、`7套件→8套件(+runtime-ab)`、`100%一致→判定彼此一致`、`65项→约57项(本地,test_api需MySQL)`、`断言→验证`。
