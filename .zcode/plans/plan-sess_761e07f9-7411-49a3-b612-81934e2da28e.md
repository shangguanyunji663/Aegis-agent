## 第六轮：回复"真人化"改造（含前置提交与轮次文档）

### 第 0 步：提交推送现有未提交改动
- `fix/source-labels` 分支上未提交的 `app/agents/classic.py`（compose_plan 轻量化整理）与 `app/llm/client.py`（LLM 网络诊断日志：耗时统计/失败告警）先单独 commit + push。
- 轮次确认（已核实）：REFACTORING(一)→OPTIMIZATION(二)→AUTH-MYSQL(三)→LANGGRAPH-DOCKER(四)→DEEP-ENHANCEMENTS(五)，本次为**第六轮**。

### 第 1 步：重写 `app/llm/prompts.py`
- 系统提示词：身份改为"你是 Aegis，校园心理支持助手"（禁止自称"生成器/产品组件"）；保留安全边界（不诊断/不承诺保密/不泄露内部风险分与审计细节）。
- 用户提示词：保留现有字段，把僵硬的"先共情→1-3步骤→开放问题"三段式换成灵活对话指导（贴对方的话说、口语短句、长度匹配、不强制列表、不必每条以提问收尾、一次最多一问、建议≤两条）。
- 加防泄漏指示：历史摘要等字段仅内部参考，回复禁止出现"用户提到""系统回应重点"等标签、不得逐字复述。

### 第 2 步：修 `app/agents/classic.py` 兜底模板
- 删除整行粘贴记忆原文（`我会结合你前面提到的情况继续陪你梳理：{latest_memory}`）；改为提取记忆行"用户提到："到"；系统回应重点："之间的用户原话自然织入（保证 test_orchestrator.py:328 "考试压力"仍命中）。
- 开场/收尾按意图区分：COMPANION 简短口语（不再追问"最难受的时刻"）；HIGH 风险安全话术原样不动。
- `finalize_plan` 降级时加 `logger.warning`（provider/model），便于排查 LLM 间歇失败。

### 第 3 步：`app/config.py` + `app/llm/client.py`
- Settings 新增 `llm_support_temperature: float = 0.6`；两个客户端的 support 生成/流式改用该温度（替换硬编码 0.2）；风险/改写/评审保持 0.0。

### 第 4 步：测试同步
- `test_orchestrator.py:315` 改断言新开场白（保留 `fallback:mock` trace 断言）。
- 新增守护测试：兜底回复不含"用户提到"/"系统回应重点"；`build_messages` 不含"咨询回复生成器"且含防复读指示。
- 跑全量 pytest 确认无回归。

### 第 5 步：编写第六轮 md 并提交推送
- 新建 `docs/records/`（沿用 DEEP-ENHANCEMENTS.md 的结构风格：标题+分支/时间/系列链+验证数据、背景、逐项变更、验证结论）。
- 内容：本轮问题诊断（兜底模板泄漏内部记忆、提示词八股、温度过低）+ 上述全部改动 + 测试结果。
- 最后将本轮代码+md 一起 commit + push 到 `fix/source-labels`。