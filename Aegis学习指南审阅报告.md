# 《Aegis 项目逐文件学习指南》审阅报告

> 审阅日期：2026-09-02 ｜ 审阅基线：`main` 分支 `8b6284d`（ROUND-18 后），文档为未提交修改版
> 审阅方法：通读全文 2490 行；对文档中 20+ 处代码声明逐一与源码比对（含 `app/` 全部 62 个 `.py` 文件清单、行数、枚举、签名、行号引用）

---

## 一、总体结论（先行）

**基本达标，但未完全达标。**

这份指南在同类文档中已属上乘：四条设计哲学贯穿全书、每站"为什么先写它/动手试一试/常见易错点/练习"三件套齐全、选型理由带替代方案与代价、FAQ 承担跨模块因果解释，"None 即兜底""治理与业务正交"等思想传递是真实有效的。**按顺序读完，读者能掌握约 90% 的 agent 后端核心代码并理解设计思路。**

但距"完整掌握全部核心代码"还差四类改动：

| # | 关键改动 | 影响维度 |
| --- | --- | --- |
| 1 | 修正 5 处与源码不一致的事实声明（详见第二节） | 覆盖度 + 可跟随性（练习 2 会直接翻车） |
| 2 | 补 `store.search_knowledge` 检索流水线"总装"源码走读 | 覆盖度 + 学习路径（9↔10 站断层） |
| 3 | 给 8.4 LangGraph 运行时补源码片段与 try-it（全书唯一无代码锚点的运行时） | 思路传递 |
| 4 | 补 `app/api/errors.py` 全局异常处理走读 + ROUND-18 主题持久化后端小节 | 覆盖度（两处完全空白） |

---

## 二、与源码不一致的事实声明（必须修正）

以下均已逐一与当前 `main` 源码核对：

| # | 文档位置 | 文档声明 | 源码实际 | 读者后果 |
| --- | --- | --- | --- | --- |
| 1 | 2.1 矩阵、1.3 节、第 1 站练习 2 | entities.py「19 张表」「19 个 Base 子类」 | **20 张表**：ROUND-18 主题切换新增 `UserPreference`（entities.py:255） | 练习 2「数表与 19 对应」必然数出 20，读者怀疑自己或文档 |
| 2 | 7.1 节 | AgentEventType「真实枚举全集」列出 12 个 | **13 个**：遗漏 `MESSAGE_SENT`（events.py:15） | 按"全集"自建协议时少一个事件类型 |
| 3 | 第 10 站开头、2.1 矩阵 | store.py「约 1082 行」 | **1173 行**（74 个方法） | 数字性失真，累积损伤文档公信力 |
| 4 | 12.5 节 | admin.py「约 248 行」 | **315 行** | 同上 |
| 5 | 12.5 节 | auth_routes.py「登录/登出/当前用户」 | 另有**主题读写端点**（auth_routes.py:93、105 调 `store.get/set_user_theme`） | 按 12.5 的路由清单对照源码对不上 |

> 正面确认（抽查通过、无需改动）：`add_artifact` 行号引用（events.py:152）✅、`SkillRegistry(knowledge_dir, report_sink, knowledge_search, settings)` 签名 ✅、`HIGH_TERMS` 18 词与三层词表设计 ✅、`RuntimeEvent.sse_event` 映射 ✅、`orchestrator.handle/handle_stream` 公开方法 ✅、`_token_chunks`/`live_tokens["count"]==0` 判据 ✅、`rewrite_query` 兜底 `[:60]` ✅、harness 8 套件名 ✅、tests/ 15 个文件清单 ✅、24 篇知识文档 ✅、RRF 不叠加 rerank 与 Redis 缓存只写不读 ✅。

---

## 三、逐维度评估

### 维度 1：覆盖度 —— ★★★★☆（约 90%，有 4 处实质缺口）

**已覆盖**：14 站对应 app/ 全部核心目录；核心数据结构（Intent/RiskLevel/AssessmentResult/SkillSpec/LLMContext/AgentTask/AgentArtifact/CollaborationBlackboard/ToolContract/GraphState 等）均有展开；四条主调用链（学生对话、风险闭环、工具治理、SSE 流式）有时序图 + 源码走读。

**缺口（按重要性排序）**：

1. **`app/api/errors.py` 完全未覆盖**（50 行）。全局异常分层约定：`ToolGovernanceError→403`、`ValueError→400`、校验失败→422、兜底 500 不泄内部细节。它是第 11 站治理链的"最后一环"——`governed_payload` 抛出的异常如何变成 HTTP 语义返回给双端，目前断层；也是"治理与业务正交"哲学在 HTTP 侧的落点。
2. **ROUND-18 主题持久化后端整段缺失**。文档脚注声称对齐 ROUND-18，但该轮的后端部分——`UserPreference` 表（第 20 张）、`store.get_user_theme/set_user_theme`（store.py:314-335，含 `THEME_CHOICES` 白名单校验）、auth_routes 主题端点、`pages.py` 服务端主题注入（`_resolve_theme` + 先于 CSS 的内联脚本防首屏闪烁）——零覆盖。连带造成第二节 5 处过时声明。
3. **`store.search_knowledge` 总装走读缺位**（9.6 只有一句"（第 10 站）串成完整流水线"，第 10 站只一行带过）。该方法的工程细节——向量异常回退（`vector_required` 分支）、向量候选双 key 匹配（db_id 与 source:index）、weighted/RRF 分支与 rerank 条件、邻块扩展、top_k 截断、缓存读写点——只在 9.7 以散文描述，无一次带行号的代码走读。RAG 是项目三大卖点之一，"算法讲透了、总装没人带"。
4. **`eval/run_eval.py` 未走读**。仅作为命令出现（4.7/13 站）；它与 `harness/factory.py` 共用装配工厂只点了一句，综合评测的组织方式（装配顺序、算哪些指标组）没有展开。
5. 小项：`rag/chunking.py:74` 的规则版 `rewrite_query`（检索词归一化）未提——与 `KnowledgeAgent.rewrite_query`（LLM 语义改写）构成"两次改写"，文档未区分，读者会混淆 9.6 流水线第一步"改写查询"指哪一个。`app/init_db.py`（30 行）在 4.4 已覆盖，可接受。

### 维度 2：学习路径 —— ★★★★☆（顺序科学，两处断点）

**已达标**：第零部分术语先行 → 架构 → 职责矩阵 → 选型 → 环境 → 14 站按依赖图排序（数据形状→安全底座→规则风险→技能→模型→单轮 Agent→自治协作→编排→RAG→存储→工具→HTTP→评测→收尾）→ FAQ → 自检清单。前向引用都有明确路标（如 9.6 标注"第 10 站"、第 7 站的循环导入警告与"orchestrator 优先"口诀）。

**断点**：

1. **9 ↔ 10 站之间的"总装"无归属**：检索流水线的实现在 store.py（第 10 站文件），但第 9 站说"见第 10 站"、第 10 站又只一行——读者在两站都找不到 `search_knowledge` 的走读，被迫自己翻源码，违背"不需反复回查源码"的目标。建议在 9.7 之后新增 **9.8「search_knowledge 总装走读」**（贴源码 + 行号注释），第 10 站改为一句话指向 9.8。
2. **第 1 站练习 2 的"19 张表"硬伤**（见第二节 #1）：这是全书唯一会让读者"动手即翻车"的练习，优先修正。
3. ROUND-18 主题后端缺失造成两处对照断点：按 1.3 的表清单、12.5 的路由清单对照源码，均会多出不认识的 `UserPreference`/theme 端点。

### 维度 3：思路传递 —— ★★★★★（全文档最强项，仅一处单薄）

**已达标**：四条设计哲学（1.2）在每个站的"学习要点"回收；Part 3 每项选型均给"替代方案 + 代价/取舍"；FAQ Q1/Q3/Q4/Q5/Q6 是高质量的跨模块因果解释；7.1"为什么克隆式不可变"、9.5"为什么 SCD-2"、11 站"为什么不让模型直接调工具"都讲到了动机层。重构对比（45 路由闭包 → app.state、board 三份拷贝 → 参数化收编）传递了"怎么想"而不只是"是什么"。

**单薄点**：

1. **8.4 LangGraph 运行时是全书唯一没有一段源码、没有专属 try-it 的运行时**——只有一整段文字。对比 7.1（黑板有代码+行号）、8.1（有序路径有分流代码），三运行时这一核心卖点中最"工业级"的一档反而无代码锚点：`GraphState` 的 `Annotated[list, operator.add]` 增量合并、条件边声明、`SqliteSaver` 检查点都只诉诸文字。建议补 15~25 行带注释的 `build_graph()` 片段 + 把第 8 站练习 1（切 `agent_runtime=langgraph` 重跑）配一段预期 trace 形态差异示例。
2. errors.py 的"分层异常约定"是可与"治理与业务正交"并列的工程思想，缺失后第 11→12 站之间有一环断层（同维度 1 缺口 1）。
3. "两次检索词改写"的职责区分未讲（同维度 1 缺口 5）。

### 维度 4：可跟随性 —— ★★★★☆（三件套完备，三处动手锚点缺失）

**已达标**：每站三件套齐全；try_station1~12 可复制运行且预期输出具体；导入顺序、TestClient 读 SSE 的 `client.stream` 细节、"指标随版本浮动"的诚实标注，都是对初学者极友好的细节。

**缺口**：

1. 第 1 站练习 2 因"19 张表"翻车（第二节 #1）。
2. 9.7 讲完融合/重排后**没有"亲手跑一次流水线"的示例**——补一个 4 行的 try-it：构造 `DatabaseStore` 后直接调 `store.search_knowledge("考试压力", top_k=3)`，再切 `KNOWLEDGE_FUSION_MODE=rrf` 对比两次输出差异（呼应 9.7.2 的"两模式预期"）。
3. 8.4 LangGraph 无 try-it（见维度 3）。
4. 若补主题后端小节，顺带补一个 TestClient 主题读写往返示例（PUT theme → 重新 GET 页面看 `data-theme` 注入）。

---

## 四、修改建议清单（按对学习目标的影响程度排序）

| 优先级 | 改动 | 落点 | 工作量 |
| --- | --- | --- | --- |
| P0 | 修正 5 处事实声明：表数 19→20（含练习 2 改为 20 并把 `UserPreference` 加入 1.3 表清单）、AgentEventType 全集补 `MESSAGE_SENT`、store.py 1082→1173、admin.py 248→315、auth_routes 职责补主题端点 | 2.1 / 1.3 / 第 1 站练习 / 7.1 / 第 10 站 / 12.5 | 极小 |
| P0 | 新增 9.8「search_knowledge 总装走读」：贴 store.py:803-905 源码，按"缓存→向量(含异常回退)→元数据过滤→BM25→加权/RRF 分支→条件 rerank→邻块扩展→截断→写缓存"逐段注释；第 10 站对应处改指向 9.8 | 9.8（新增）、第 10 站 | 小 |
| P1 | 8.4 补 LangGraph 源码锚点：`GraphState` 定义 + 条件边 + `SqliteSaver` 挂载约 20 行带注释片段；给第 8 站练习 1 配 langgraph/ordered 的预期 trace 形态差异样例 | 8.4 | 小 |
| P1 | 新增 errors.py 走读（建议作为 12.3b「全局异常处理」）：贴 `register_exception_handlers` 全文，讲"路由显式 HTTPException / 领域异常集中映射 / 兜底不泄内部"三层约定，并回连 11.1 的 `ToolGovernanceError→403` | 第 12 站 | 小 |
| P2 | 新增「ROUND-18 主题持久化」小节：UserPreference 表 → store.get/set_user_theme（THEME_CHOICES 白名单）→ auth_routes 主题端点 → pages.py 服务端注入防闪烁；同步更新 1.3/2.1/12.5 | 第 1/10/12 站各补一段或集中一节 | 中 |
| P2 | 第 13 站补 eval/run_eval.py 装配走读（与 harness/factory.py 的共用关系、指标组织） | 第 13 站 | 小 |
| P3 | 9.3 补 `rewrite_query`（chunking.py:74 规则归一化）说明，并在 9.6 明确"流水线第一步的改写 = 规则归一化，KnowledgeAgent 的 LLM 改写发生在更上游" | 9.3 / 9.6 | 极小 |
| P3 | 补 9.7 的 search_knowledge 双模式对比 try-it（weighted vs RRF 同查询输出差异） | 9.7 末尾 | 极小 |

---

## 五、达标判定

- **当前状态**：覆盖度 ~90%、学习路径与可跟随性存在少量断点、思路传递优秀。按"顺序从头读到尾即可完整掌握所有 agent 后端核心代码"的严格标准，**未达标**——缺口集中在：检索流水线总装、LangGraph 源码锚点、全局异常处理、ROUND-18 主题后端四处空白，以及 5 处会误导读者的事实声明。
- **达标条件**：完成上表 P0×2 + P1×2（约半天工作量）即可达到"完整覆盖 + 可跟随无卡点"；P2/P3 完成后可达"与源码逐文件对齐、随时可对外展示"的水准。
- **保留优点**：14 站结构、三件套、FAQ、Part 3 选型论证、四哲学贯穿——这些是本指南的核心资产，修改时不要重排，只做定点增补与数字勘误。

***

# 第二轮复审记录（2026-09-02 23:20，修订完成后）

> 复审基线：修订后的指南（2490→2641 行，含 9.8/12.3b/12.5b/8.4 锚点、13 站 run_eval 等全部 P0~P3 落地）。
> 复审目标线（用户本轮强调）：**一个只具备基础后端知识的读者，按顺序读完后能够复现整个后端**。
> 复核方法：对新增的 4 节逐一比对源码；对全库路由/模型做了一次清点（48 端点、11 个 schema、8 个 MCP 工具）；当场修正一处数字遗留（schemas「9 个请求模型」→ 11，schemas.py 实为 11 个类）。

## 复审结论（先行）

- **相对最初目标（掌握 agent 核心代码 + 理解设计思路）：已达标。**
- **相对"学完能复现整个后端"的目标：核心链路可复现，但还差 1 处 P1 + 2 处 P2 + 2 处 P3**，补齐后可达"功能级复现"（按 14 站重写 → 用总结四清单 + 附录 A 端点逐一验收）。

## 四维度复审

| 维度 | 上轮 | 本轮 | 说明 |
| --- | --- | --- | --- |
| 1 覆盖度 | ★★★★☆（90%） | ★★★★★（~97%） | 上轮 4 处空白（errors.py/主题后端/search_knowledge 总装/LangGraph 锚点）已补齐；残余 3 处"点到为止"见下 |
| 2 学习路径 | ★★★★☆ | ★★★★★ | 9↔10 断层已消除（9.8 承担总装走读）；12.3b/12.5b 插入位置自然；练习 2 翻车点已修 |
| 3 思路传递 | ★★★★★ | ★★★★★ | 异常分层、LangGraph 设计对照、主题白名单取舍已补；仅余一处选型论证空白 |
| 4 可跟随性 | ★★★★☆ | ★★★★★ | 9.8 双模式对比、12.5b TestClient 验证均为可运行示例；LangGraph 补齐了全书唯一代码盲区 |

## 残余薄弱位置（按对复现目标的影响排序）

### 1.（P1）第 11 站 11.3 tool_queue 无任何源码——治理闭环里唯一"零代码"的一环
- 位置：11.3「services/tool_queue.py」全是要点式条目，零代码块。
- 复现影响：worker 的**失败语义**（这是治理的价值所在）读者只能靠描述猜：`attempts+1 → 未超限改回 PENDING 并设 run_after 退避 → 超限写死信（job_public_id 去重）`、`_dependency_ready`（同 case 的 handoff 完成后 email 才发）、`run_once` 批量领取 + 分布式锁、线程轮询循环。service 内部每个动作都对应 store 的状态机，没有代码打样时"重写一个行为一致的 worker"是最容易跑偏的一站。
- 建议：补 15~25 行带行号注释的失败路径走读（tool_queue.py:51-160），可运行示例用 `always_fail` 载荷跑 `run_once` 两次，观察 `attempts` 递增与死信产生（11.4 的测试钩子，练习 2 已有但无打样）。

### 2.（P2）无 API 契约总表——"功能级复现"缺最后一块拼图
- 位置：12.5 对 admin 只是定性描述（"待审报告、个案、工具队列/死信、知识库、评测、审计"），全库 48 个端点无一被完整列举（chat 7 / system 4 / auth 5 / pages 3 / admin 29）。
- 复现影响：读者重写的后端"行为一致"，但端点的 path/动词/权限/请求体要逐个翻源码（admin.py 315 行、schemas 11 个类），违背"不需反复回查源码"。
- 建议：新增「附录 A：后端路由与 Schema 总表」——48 端点 × {方法/路径/鉴权/请求体} 表格 + 11 个 schema 字段要点，从现有 5 个路由文件直接生成；12.5 的定性文字保留并指向附录。

### 3.（P2）第三部分缺「自建 DB 任务队列 vs Celery/消息中间件」选型论证
- 位置：Part 3 选型清单覆盖框架/检索/Redis/SSE/MCP/评测，唯独没有回答"为什么用 ToolJob 表 + 轮询 worker，而不是 Celery/RQ"。
- 复现影响：这是读者自建时最容易踩坑的决策点——DB 队列的取舍（零依赖、任务即数据可审计、失败可 SQL 查询 vs 吞吐上限、需要轮询、无延迟语义）不讲清，"照着做"会变成"不知道为什么这么做"。
- 建议：Part 3 加一条，按既有"选了/为什么/替代/代价"四段式（与 11.3 的机制描述呼应）。

### 4.（P3）mcp/server.py 零代码（可选后端）
- 位置：11.6 只描述职责与"协议变治理不变"，8 个 `@mcp.tool`（server.py:116-197）无示例。
- 影响小：MCP 是可选增强且治理边界已讲透；补一个 8 行 `@mcp.tool` 骨架 + `python -m app.mcp.server --list` 输出即可闭环。

### 5.（P3）`app/init_db.py` 未单列（30 行）
- 4.4 已说明其行为；可维持现状，或在 4.4 补 3 行源码引用。不构成复现障碍。

## 数字勘误（本轮当场修正）

- schemas.py 实为 **11 个**请求模型（ChatRequest/ReportUpdate/SessionCreate/SessionRename/CaseNote/CaseStatusUpdate/KnowledgeIngest/KnowledgeUpload/Login/Register/ThemeRequest），文档原写"9 个"——已在 12.5 修正并补一句与领域模型的分工说明。

## 修改建议汇总（按影响排序）

| 优先级 | 改动 | 落点 | 工作量 |
| --- | --- | --- | --- |
| P1 | tool_queue 失败路径源码走读 + always_fail 死信示例 | 11.3 | 中 |
| P2 | 附录 A：48 端点 + 11 schema 的契约总表（按路由文件生成） | 新增附录（可挂第七部分后） | 中 |
| P2 | Part 3 补「自研 DB 队列 vs Celery/消息中间件」条目 | 第三部分 | 小 |
| P3 | 8 行 `@mcp.tool` 骨架示例 + `--list` 输出 | 11.6 | 极小 |
| P3 | 4.4 补 init_db.py 三行源码引用（可选） | 4.4 | 极小 |

## 达标判定（终版）

- **最初目标（掌握 + 理解）**：✅ **已达标**——14 站顺序通读即可掌握全部 agent 核心代码与设计逻辑，无再需回查源码的断点。
- **复现目标（学完能重建整个后端）**：⚠️ **差 1 项 P1 + 2 项 P2**——当前"能重建出行为一致的核心链路"，补上 worker 失败语义走读（P1）与 API 契约总表（P2）后即达"功能级复现"；Part 3 队列选型论证（P2）决定复现者"是否真的理解了为什么"，建议一并补。
- 复现验收路径（建议写进总结四）：按 14 站重写 → `python -m app.init_db` + 4.5 三步闭环 → `python -m pytest tests -q` → `python -m app.evaluation.harness.runner --suite all` → 对照附录 A 逐端点冒烟。

## 修订状态（2026-09-02 23:30）

上表 P1 + P2×2 + P3 已全部落地到指南（2641→2788 行）：11.3 补 `run_job` 失败状态机走读与 always\_fail 死信示例；Part 3 补「自研 DB 队列 vs Celery/RQ」条目；11.6 补 `@mcp.tool` 骨架示例；新增附录 A（48 端点契约总表，锚点 appendix-a，已入总目录）；总结四补复现验收自检项；另勘误 schemas「9 个请求模型」→ 11。附录内容逐一核对 admin.py/system.py/chat.py/auth_routes.py 源码（含 knowledge/search 的 q/top_k/topic 参数、upload 的安全文件名校验、eval-results/run 调 run_evaluation）。**复现目标现可判定：达标（功能级）**——仍需人工验证的只剩读者自建后跑一遍附录 A 冒烟。
