"""Optional MCP-facing tool boundary for Aegis.

The default product path uses the internal ToolJob queue. This package exposes
the same governed contracts in a dependency-light shape so local demos and tests
can validate the extension boundary without requiring a running MCP host.
"""
